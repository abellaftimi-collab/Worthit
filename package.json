{
  "name": "worthit",
  "version": "1.0.0",
  "description": "Worthit — the impulse-buy blocker. Site + Stripe backend + AI agent.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "og": "node scripts/make-og.js",
    "ext-icons": "node scripts/make-ext-icons.js",
    "press": "node scripts/make-press.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@amplitude/unified": "^1.1.27",
    "@supabase/supabase-js": "^2.110.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "stripe": "^17.0.0"
  }
}
